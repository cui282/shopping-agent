# Keep live and sandbox results separate

User-facing research tasks use either live Marketplace Gateway data or disclosed sandbox fixtures,
never both. Although fixture fallback can make a partial live search look more complete, mixing the
sources weakens comparison integrity and can make simulated products appear equivalent to current
marketplace offers; failed live marketplaces therefore remain visibly unavailable, while any mixed
mode is restricted to explicitly enabled developer diagnostics.
