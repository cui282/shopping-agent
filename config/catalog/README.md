# Catalog mappings

These YAML files describe aliases and category mappings for licensed data-provider feeds. They
are applied before gateway normalization, so provider schema changes do not require changing the
normalizer. They do not contain marketplace credentials or live records.
